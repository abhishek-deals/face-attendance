# Face Attendance System — Vercel Requirements
# Registration: face detection done in browser (no OpenCV needed)
# Training: uses Pillow + numpy (lightweight)
# Recognition: uses OpenCV contrib for LBPH model (live attendance page)

# OpenCV for training and live recognition
opencv-contrib-python-headless==4.9.0.80

# Scientific computing
numpy>=1.24.0,<2.0.0

# Image processing (used during training)
Pillow>=10.0.0

# Web framework
Flask>=3.0.0

# PostgreSQL driver for persistent cloud storage (Neon)
psycopg2-binary>=2.9.0
